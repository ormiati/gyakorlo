'use strict'

console.log('1. feladat:');

const products= ['sajt', 'tej', 'túró', 'tej', 'sajt'];

function clean (prod) {
const clearprod = new Set();
for(let i=0; i<prod.length; i +=1)
clearprod.add(prod.value[i]);
const newperod = clearprod;
console.log (newperod);
};

function clean(products);